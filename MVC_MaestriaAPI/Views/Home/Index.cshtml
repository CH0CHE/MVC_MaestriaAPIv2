<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <title>Redirigiendo...</title>
    <meta http-equiv="refresh" content="0; URL=http://localhost:5047/Home/Dashboard" />
</head>
<body>
    Si no te redirige automáticamente, haz clic
    <a href="http://localhost:5047/Home/Dashboard">aquí</a>.
</body>
</html>

<!--
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Dashboard de Datos</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f6f8;
            margin: 20px;
        }

        h2 {
            color: #333;
        }

        table {
            border-collapse: collapse;
            width: 100%;
            margin-bottom: 20px;
            background-color: white;
        }

        th, td {
            border: 1px solid #ccc;
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #007BFF;
            color: white;
        }

        .charts-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 30px;
        }

        canvas {
            background-color: white;
            border: 1px solid #ccc;
            padding: 10px;
        }

        .gauge-container {
            width: 300px;
            height: 300px;
            margin: auto;
        }
    </style>
</head>
<body>

    <h2>Tabla de Registros</h2>
    <table id="data-table">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Valor 1</th>
                <th>Valor 2</th>
            </tr>
        </thead>
        <tbody>
            <tr><td>Ana</td><td>80</td><td>70</td></tr>
            <tr><td>Juan</td><td>90</td><td>85</td></tr>
            <tr><td>Luisa</td><td>75</td><td>60</td></tr>
            <tr><td>Carlos</td><td>95</td><td>92</td></tr>
            <tr><td>Elena</td><td>65</td><td>70</td></tr>
        </tbody>
    </table>

    <div class="charts-container">
        <canvas id="barChart" width="400" height="300"></canvas>
        <canvas id="lineChart" width="400" height="300"></canvas>
    </div>

    <h2>Tabla de Promedios</h2>
    <table id="avg-table">
        <thead>
            <tr><th>Promedio Valor 1</th><th>Promedio Valor 2</th></tr>
        </thead>
        <tbody>
            <tr><td id="avg1"></td><td id="avg2"></td></tr>
        </tbody>
    </table>

    <h2>Estado del Promedio</h2>
    <div class="gauge-container">
        <canvas id="gaugeChart" width="300" height="300"></canvas>
    </div>

    <script>
        const rows = document.querySelectorAll("#data-table tbody tr");
        const nombres = [], valor1 = [], valor2 = [];

        rows.forEach(row => {
          const cols = row.querySelectorAll("td");
          nombres.push(cols[0].textContent);
          valor1.push(Number(cols[1].textContent));
          valor2.push(Number(cols[2].textContent));
        });

        const avg1 = valor1.reduce((a,b) => a + b, 0) / valor1.length;
        const avg2 = valor2.reduce((a,b) => a + b, 0) / valor2.length;

        document.getElementById("avg1").textContent = avg1.toFixed(2);
        document.getElementById("avg2").textContent = avg2.toFixed(2);

        new Chart(document.getElementById("barChart"), {
          type: 'bar',
          data: {
            labels: nombres,
            datasets: [
              { label: "Valor 1", data: valor1, backgroundColor: '#007BFF' },
              { label: "Valor 2", data: valor2, backgroundColor: '#FFC107' }
            ]
          },
          options: { responsive: true }
        });

        new Chart(document.getElementById("lineChart"), {
          type: 'line',
          data: {
            labels: nombres,
            datasets: [
              { label: "Valor 1", data: valor1, borderColor: '#007BFF', fill: false },
              { label: "Valor 2", data: valor2, borderColor: '#FFC107', fill: false }
            ]
          },
          options: { responsive: true }
        });

        // Gráfico de tipo gauge simulado con doughnut
        const promedioGeneral = (avg1 + avg2) / 2;
        let estado = "";
        let color = "";

        if (promedioGeneral >= 80) {
          estado = "Alto";
          color = "#28a745";
        } else if (promedioGeneral >= 60) {
          estado = "Normal";
          color = "#ffc107";
        } else {
          estado = "Bajo";
          color = "#dc3545";
        }

        new Chart(document.getElementById("gaugeChart"), {
          type: 'doughnut',
          data: {
            labels: [estado, ""],
            datasets: [{
              data: [promedioGeneral, 100 - promedioGeneral],
              backgroundColor: [color, "#e9ecef"],
              borderWidth: 1
            }]
          },
          options: {
            rotation: -90,
            circumference: 180,
            cutout: "70%",
            plugins: {
              legend: { display: true },
              tooltip: { enabled: false }
            }
          }
        });
    </script>
</body>
</html>

-->
